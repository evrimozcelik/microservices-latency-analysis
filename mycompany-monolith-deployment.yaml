apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: mycompany-monolith
spec:
  replicas: 1
  template:
    metadata:
      labels:
        run: mycompany-monolith
    spec:
      containers:
        - name: mycompany-monolith
          image: registry.eu-gb.bluemix.net/evrim/com.mycompany.services
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "500m"
            limits:
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: mycompany-monolith-service
  labels:
    run: mycompany-monolith
spec:
  type: NodePort
  selector:
    run: mycompany-monolith
  ports:
   - name: "service"
     protocol: TCP
     port: 8080
     targetPort: 8080
     nodePort: 30080
